void downloadTextFile(String content, String filename) {
  // Not implemented for non-web platforms yet.
  // ignore: avoid_print
  print(
    'Download requested for $filename, but not supported on this platform.',
  );
}
